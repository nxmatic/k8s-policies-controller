apiVersion: apps/v1
kind: Deployment
metadata:
  name: node-controller
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/instance: node-controller
      app.kubernetes.io/name: k8s-policy-controller
  template:
    metadata:
      labels:
        app.kubernetes.io/instance: node-controller
        app.kubernetes.io/name: k8s-policy-controller
    spec:
      containers:
      - args:
        - --zap-devel
        - --kind=node
        env:
        image: k8s-policy-controller
        imagePullPolicy: Always
        livenessProbe:
          httpGet:
            path: /metrics
            port: metrics-server
        name: controller
        ports:
        - containerPort: 9443
          name: webhook-server
          protocol: TCP
        - containerPort: 8080
          name: metrics-server
          protocol: TCP
        readinessProbe:
          httpGet:
            path: /metrics
            port: metrics-server
            scheme: HTTP
        resources: {}
        securityContext: {}
        volumeMounts:
        - mountPath: /tmp/k8s-webhook-server/serving-certs
          name: cert
          readOnly: true
      securityContext: {}
      serviceAccountName: node-controller
      volumes:
      - name: cert
        secret:
          defaultMode: 420
          secretName: node-controller-tls
